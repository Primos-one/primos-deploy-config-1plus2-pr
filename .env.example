# Copy to .env and fill in values:
# cp .env.example .env
DEPLOY_K3S_PROJECT_NAME=DEMO-DO-NOT-USE-1plus2
PULUMI_CONFIG_PASSPHRASE=
ARGOCD_ADMIN_PASSWORD_HASH=

# v3-primos branch (stacks/Pulumi.dev.yaml) additional seams:
# Embedded-Gitea creds for the PRIVATE internal apps repo (ArgoCD repo-secret).
# Values come from the encrypted Portal config (#419) via the sealed .env half
# (ADR-002) — never commit real values.
GITEA_ADMIN_USER=gitea_admin
GITEA_ADMIN_PASSWORD=
# Optional overrides:
# INTERNAL_CONFIG_REPO_URL=https://gitea.cp.benadis.org/deploy-config/primos-deploy-config-1plus2-pr.git
# PRIMOS_DEPLOY_SSH_KEY_PATH=/path/to/staged/deploy_key
